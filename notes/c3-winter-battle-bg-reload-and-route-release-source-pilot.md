# C3 event script source pilot

## Summary

- Family: `winter-battle-bg-reload-and-route-release`.
- Source: `src/c3/event_scripts/winter_battle_bg_reload_and_route_release.asar.asm`.
- Manifest: `build/c3-winter-battle-bg-reload-and-route-release-source-pilot.json`.
- Spans emitted: 1.
- Bytes represented: 429.
- Instructions represented: 115.
- ROM byte validation: PASS.

High-ranked terminal source-pilot batch unlocked by the no-argument `C0:18F3` reload-map callback and the two-word `C0:A87A` camera-relative anchor wrapper. This covers the rest of ebsrc script 340's battle-background transition continuation plus scripts 341-345: reload branches, fade/tick callback cleanup, camera-relative actor setup, compass-facing presentation, and a rightward movement/text halt.

The source is not wired into `src/c3/bank_c3_helpers_asar.asm` yet. That is deliberate: this pass proves the representation and byte validation before replacing generated byte corridors in the bank scaffold.

## Covered Spans

| Address | Label | Bytes | Instructions |
| --- | --- | ---: | ---: |
| `C3:199E..C3:1B4B` | `ContinueEvent340_BattleBgTransition` | 429 | 115 |

## Validation

- Every emitted span was decoded over its exact byte range and revalidated against the ROM bytes used to generate it.

## Next Promotion Step

Continue only after pinning the next callback blocker in the larger Winters corridor, or move to another ready seam from `notes/c3-source-pilot-frontier.md`.
