; EarthBound C2 Run final prayer damage phase 4.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C2:C623..C2:C64C BTLACT_GIYGAS_PRAYER_4
;
; Runtime contract:
; - Final Prayer action row 294 / phase 4.
; - Displays `C7:BB38` through the shared stage transition helper, applies
;   prayer damage amount `0x00C8` through `C2:C3E2`, then advances `$A97A` to
;   phase `8`.

; ---------------------------------------------------------------------------
; External contracts used by this module

C2C37A_RunFinalPrayerStageTransition = $C37A
C2C3E2_ApplyFinalPrayerDamageStep    = $C3E2

; ---------------------------------------------------------------------------
; C2:C623

BTLACT_GIYGAS_PRAYER_4:
C2C623_RunFinalPrayerDamagePhase4 = BTLACT_GIYGAS_PRAYER_4
    rep #$31
    phd
    tdc
    adc.w #$FFEE
    tcd
    lda.w #$BB38
    sta $0E
    lda.w #$00C7
    sta $10
    ldx.w #$00B9
    lda.w #$01DF
    jsr C2C37A_RunFinalPrayerStageTransition
    lda.w #$00C8
    ; Third prayer damage tier: 200.
    jsr C2C3E2_ApplyFinalPrayerDamageStep
    lda.w #$0008
    sta $A97A
    pld
    rtl
