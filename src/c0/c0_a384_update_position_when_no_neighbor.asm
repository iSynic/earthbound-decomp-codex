; EarthBound C0 Update Position When No Neighbor.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:A384..C0:A39E UpdatePosition_WhenNoNeighbor

; ---------------------------------------------------------------------------
; External contracts used by this module

; No named external contracts were supplied or recognized.

; ---------------------------------------------------------------------------
; C0:A384

C0A384_UpdatePosition_WhenNoNeighbor:
    ldx $88
    lda $2C5E,X
    db $30, $15
    lda $28DA,X
    and.w #$00D0
    beq C0A396_UpdatePosition_WhenNoNeighbor_LA396
    jmp $98F2
C0A396_UpdatePosition_WhenNoNeighbor_LA396:
    ldx $88
    lda $289E,X
    db $30, $03
    rts
