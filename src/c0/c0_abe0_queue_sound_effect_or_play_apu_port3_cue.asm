; EarthBound C0 Queue Sound Effect Or Play Apu Port3 Cue.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:ABE0..C0:AC0C QueueSoundEffectOrPlayApuPort3Cue

; ---------------------------------------------------------------------------
; External contracts used by this module

; No named external contracts were supplied or recognized.

; ---------------------------------------------------------------------------
; C0:ABE0

PLAY_SOUND:
C0ABE0_QueueSoundEffectOrPlayApuPort3Cue = PLAY_SOUND
    sep #$30
    cmp.b #$00
    beq PLAY_SOUND_UNKNOWN0
    ldx $00CA
    ora $1ACA
    sta $1AC2,X
    txa
    inc A
    and.b #$07
    sta $00CA
    lda.b #$80
    eor $1ACA
    sta $1ACA
    rep #$30
    rtl
PLAY_SOUND_UNKNOWN0:
C0AC01_QueueSoundEffectOrPlayApuPort3Cue_LAC01 = PLAY_SOUND_UNKNOWN0
    sep #$20
    lda.b #$57
    sta $002143
    rep #$30
    rtl
