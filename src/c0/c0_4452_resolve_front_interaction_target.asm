; EarthBound C0 Resolve Front Interaction Target.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:4452..C0:449B Resolve_FrontInteractionTarget

; ---------------------------------------------------------------------------
; External contracts used by this module

; Public front-interaction resolver used by bank C1. Initializes `$5D62/$5D64`,
; runs the `43BC -> 42EF` front-facing probe path, and updates facing/visual
; state when a rotated facing produced the target.

; ---------------------------------------------------------------------------
; C0:4452

FIND_NEARBY_TALKABLE_TPT_ENTRY:
C04452_Resolve_FrontInteractionTarget = FIND_NEARBY_TALKABLE_TPT_ENTRY
    rep #$31
    phd
    tdc
    adc.w #$FFEE
    tcd
    lda.w #$FFFF
    ; Interaction result sentinels before probing.
    sta $5D62
    sta $5D64
    jsr $43BC
    sta $10
    cmp.w #$FFFF
    beq C04496_Resolve_FrontInteractionTarget_L4496
    ldx.w #$9889
    stx $0E
    lda $0000,X
    asl A
    tax
    lda $10
    cmp $2AF6,X
    beq C04496_Resolve_FrontInteractionTarget_L4496
    ; Accepted facing changed: commit it and refresh the slot's visual profile.
    sta $987F
    ldx $0E
    lda $0000,X
    asl A
    tax
    lda $10
    sta $2AF6,X
    ldx $0E
    lda $0000,X
    jsl $C0A780
C04496_Resolve_FrontInteractionTarget_L4496:
    lda $5D62
    pld
    rtl
