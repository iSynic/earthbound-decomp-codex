; EarthBound C0 Refresh Current Slot Profile If Composite Key Changed.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:A443..C0:A48F RefreshCurrentSlotProfileIfCompositeKeyChanged

; ---------------------------------------------------------------------------
; External contracts used by this module

; No named external contracts were supplied or recognized.

; ---------------------------------------------------------------------------
; C0:A443

C0A443_RefreshCurrentSlotProfileIfCompositeKeyChanged:
    ldx $88
    lda $2890
    clc
    adc $1A42
    lsr A
    lsr A
    lsr A
    and.w #$0001
    sta $00
    lda $2AF6,X
    asl A
    ora $00
    sta $02
    lda $2C22,X
    xba
    ora $02
    cmp $3456,X
    bne C0A468_RefreshCurrentSlotProfileIfCompositeKeyChanged_LA468
    rtl
C0A468_RefreshCurrentSlotProfileIfCompositeKeyChanged_LA468:
    sta $3456,X
    lda $00
    sta $2892
    db $80, $50
    lda $0002
    lsr A
    lsr A
    lsr A
    and.w #$0001
    sta $2892
    db $80, $42
    ldy $88
    rep #$20
    phd
    pha
    tdc
    sec
    sbc.w #$0006
    tcd
    pla
    db $80, $0D
