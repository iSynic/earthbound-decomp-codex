; EarthBound C0 Snapshot Teleport Player State To Ring.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:E196..C0:E214 SnapshotTeleportPlayerStateToRing

; ---------------------------------------------------------------------------
; External contracts used by this module

; Writes a 12-byte player snapshot into the `$5156` ring using cursor `$987D`.
; Fields used here: X/Y at +0/+2, footprint mask at +4, zero at +6, facing at +8.

; ---------------------------------------------------------------------------
; C0:E196

C0E196_SnapshotTeleportPlayerStateToRing:
    rep #$31
    phd
    tdc
    adc.w #$FFEA
    tcd
    lda.w #$987D
    sta $04
    sta $14
    ldx $04
    lda $0000,X
    sta $04
    asl A
    adc $04
    asl A
    asl A
    clc
    adc.w #$5156
    ; `$987D * 12 + $5156` selects the snapshot record.
    sta $02
    lda.w #$9877
    sta $12
    tax
    lda $0000,X
    ldx $02
    ; Snapshot live player X.
    sta $0000,X
    ldx.w #$987B
    stx $10
    lda $0000,X
    ldx $02
    ; Snapshot live player Y.
    sta $0002,X
    ldy $9889
    ldx $10
    lda $0000,X
    tax
    stx $0E
    lda $12
    tax
    lda $0000,X
    ldx $0E
    ; Snapshot footprint/collision mask at the current position.
    jsl $C05F33
    ldx $02
    sta $0004,X
    ldx $02
    stz $0006,X
    lda $987F
    ldx $02
    ; Snapshot facing.
    sta $0008,X
    lda $14
    sta $04
    ldx $04
    lda $0000,X
    inc A
    ldx $04
    sta $0000,X
    and.w #$00FF
    ldx $04
    ; Ring cursor is byte-sized.
    sta $0000,X
    pld
    rts
