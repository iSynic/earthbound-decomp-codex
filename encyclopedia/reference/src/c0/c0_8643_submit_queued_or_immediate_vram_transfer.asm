; EarthBound C0 Submit Queued Or Immediate Vram Transfer.
;
; Source-emission status:
; - Prototype level: build-candidate
; - Generated by tools/emit_linear_source_module.py from a state-aware
;   linear ROM decode, then intended for byte-equivalence validation.
;
; Source units covered:
; - C0:8643..C0:865F SubmitQueuedOrImmediateVramTransfer

; ---------------------------------------------------------------------------
; External contracts used by this module

; Common wrapper for the staged transfer ABI at `$91/$92/$94/$96/$97`.
; C0:865F chooses queued NMI DMA or immediate DMA based on `$0D`.

; ---------------------------------------------------------------------------
; C0:8643

PREPARE_VRAM_COPY_COMMON:
C08643_SubmitQueuedOrImmediateVramTransfer = PREPARE_VRAM_COPY_COMMON
    php
    rep #$20
    sep #$10
    phd
    pea $0000
    pld
    phb
    ldy.b #$00
    phy
    plb
    rep #$10
    ; Submit the staged transfer descriptor.
    jsr $865F
    plb
    pld
    plp
    rtl
    jsr $865F
    rtl
